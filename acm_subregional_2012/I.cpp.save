#include <iostream>
#include <algorithm>
#include <map>
#include <vector>
#include <cstring>
#define x first
#define y second
using namespace std;

typedef pair<int,int> per;

per p;
int t,n,m,nr[405][405];
vector<per> v;
map<int,int> mp;

int solve(int a,int b) {
  for(int i=a;)
}

int main() {
  for(cin>>t;t;--t) {
  int r=(1<<30);
    v.clear(); mp.clear();
    memset(nr,sizeof(nr));
    cin>>n>>m;
    for(int i=1; i<=n; ++i) {
      cin>>p.x>>p.y;
      v.push_back(p[i]);
    }
    sort(v.begin(),v.end());
    for(int i=0; i<v.size(); ++i) for(int j=i+1; j<v.size(); ++j) r=min(r,solve(i,j));
  }
}
