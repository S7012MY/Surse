#include <iostream>
#include <fstream>
using namespace std;

int n,m,ind[2005],v[2005];
string mt[2005];

void so(int pos,int left, int right)
{
	int A = left, B = right, i;
	for(i = left; i <= right; i++)
		if(mt[pos][ind[i]] == '0')
			v[A ++] = ind[i];
		else
			v[B --] = ind[i];
		for(i = left; i < A; i++) ind[i] = v[i];
		for(i = right; i >= A; i--) ind[A + (right - i)] = v[i];
		if(pos < n)
		{
			if(left < A - 1) so(pos+1,left, A - 1);
			if(A < right) so(pos+1,A, right);
		}
}

int main() {
	ifstream f("binar.in");
	ofstream g("binar.out");
	f>>n>>m;
	f.get();
	for(int i=0; i<n; ++i) getline(f,mt[i]);
	for(int i=0; i<m; ++i) ind[i]=i;
	so(0,0,m-1);
	for(int i=0; i<m; ++i) g<<ind[i]+1<<' ';
	return 0;
}