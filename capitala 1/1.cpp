#include <fstream>
#include <vector>
#include <queue>
#define DN 200005
using namespace std;

typedef vector<per>::iterator it;
int n,lst;
queue<int> c;
vector<int> gr[DN];

int main() {
	ifstream f("capitala.in");
	ofstream g("capitala.out");
	f>>n;
}