#include <iostream>
#include <string>
#include <set>
#include <map>
#include <vector>
#define DN 1000005
using namespace std;

int n,m,nt[105][105],d[DN],ker,mt[105][105];
vector<int> a,b;

int main() {
  cin>>n>>m;
  for(int i=0; i<m; ++i) {
    int a,b;
    cin>>a>>b;
    ++d[a]; ++d[b]; nt[a][b]=nt[b][a]=1;
  }
  for(int i=1; i<=n; ++i) if(!d[i]) ker=i;
  for(int i=1; i<=n; ++i) if(i!=ker) a.push
}