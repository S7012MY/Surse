#include <iostream>
#include <set>
#include <vector>
#include <algorithm>
using namespace std;

int n;
vector<int> v;
multiset<int> s;

int main() {
  cin>>n;
  for(int i=1; i<=n; ++i) {
    int x; cin>>x;
    v.push_back(x);
  }
  sort(v.begin(),v.end());
  for(int i=0; i<v.size(); ++i) {
    for(multiset<int>>)
  }
}
