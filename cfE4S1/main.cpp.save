#include <iostream>

using namespace std;

int main()
{
    int a,b,c,t;
    for(cin>>t;t;--t) {
      cin>>a>>b>>c;
      int mi=min(a,min(b,c)),ma=max(a,max(b,c));
      if(ma-mi>=2) {
        cout<<0;
        continue;
      }
      
    }
    return 0;
}
