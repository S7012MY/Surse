#include <iostream>
#include <vector>
#include <queue>
#include <bitset>
#define x fist
#define y second
#define mp make_pair
#define DN 1005
#define DC 105
#define inf (1<<30)
using namespace std;

typedef pair<int,int> per;
typedef vector<per>::iterator it;

int n,m,p[DN],dmin[DN][DC];
vector<per> gr[DN];

void bf(int cmax,int s, int d) {
  for(int i=0; i<n; ++i) for(int j=0; j<=cmax; ++j)
}

int main() {
  cin>>n>>m;
  for(int i=0; i<n; ++i) cin>>p[i];
  for(int i=0; i<m; ++i) {
    int a,b,c;cin>>a>>b>>c;
    gr[a].push_back(mp(b,c));
    gr[b].push_back(mp(a,c));
  }
  int q;cin>>q;
  for(;q--;) {
    int c,a,b;
    bf(c,a,b)
  }
}
