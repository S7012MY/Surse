#include <iostream>
#include <fstream>
#include <string>
using namespace std;

string s;
int nrd;

int main()
{
    ifstream f("egalitati.in");
    ofstream g("egalitati.out");
    getline(f,s);
    int rez=1;
    for(int i=0; i<s.size(); ++i) {
      if(s[i]=='(') ++nrd;
      else if(s[i]==')')
    }
    return 0;
}
