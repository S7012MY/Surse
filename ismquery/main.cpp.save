#include <iostream>
#include <fstream>
#include <cstring>
#define DN 400005
#define x first
#define y second
using namespace std;

int n,m,v[DN],ind[DN],ai[4*DN],rez[6][DN],st[6][DN],sz[6],unde[6];

int get(int nr) {
  
}

int main()
{
    ifstream f("ismquery.in");
    ofstream g("ismquery.out");
    f>>n>>m;
    for(int i=1; i<=n; ++i) f>>v[i];
    
    for(int i=n; i; --i) {
      memset(unde,0,sizeof(unde));
      for(int j=1; j<=5; ++j) rez[j][i]=get(v[i]);
    }
    return 0;
}
