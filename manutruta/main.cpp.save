#include <cmath>
#include <iostream>
#include <fstream>
using namespace std;

#define x first
#define y second

typedef pair<double, double> point;

ifstream f("lgput.in");

point a[105];

int main() {
  f >> n;
  for (int i = 1; i <= n; i++) {
    f >> a[i].x >> a[i].y;
  }

  double sol;
  a[n + 1] = 
  for (int i = 1; i <= n; i++) {
    sol += sqrt((a[i].x - a[i + 1].x) * (a[i].x - a[i + 1].x) + (a[i].y - a[i + 1].y) * (a[i].y - a[i + 1].y));
  }

  return 0;
}
