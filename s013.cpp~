#include <fstream>
#include <vector>
#include <bitset>
#define DN 2505
using namespace std;

typedef vector<int>::iterator it;

int n,m;
char mt;
vector<int> gr[DN];
bitset<DN> viz;

int main() {
	ifstream f("s013.in");
	ofstream g("s013.out");
	f>>n>>m;
	for(int i=1; i<=n; ++i) for(int j=1; j<=m; ++j) {
		f>>mt;