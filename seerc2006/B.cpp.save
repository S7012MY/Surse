#include <iostream>
#define DN 100005
using namespace std;

int n,s,sp[DN],mx[4*DN];

void build(int vn=1,int ls=1,int ld=1) {
  if(ls==ld) {
    mx[vn]=sp[ls];
    return;
  }
  int m=(ls+ld)/2,fs=2*vn;
  build(fs,ls,m);
  build(fs+1,m+1,ld);
}

int pmax(int s,int vn,int ls,int ld) {
  if(ls==ld && sp)
}

int main() {
  for(;cin>>n>>s;) {
     
     for(int i=1; i<=n; ++i) {
       int x;cin>>x;
       sp[i]=sp[i-1]+x;
     }
     build();
  }
}
