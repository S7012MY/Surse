#include <iostream>
#include <fstream>
#define DN 55
#define MOD 666013
using namespace std;

int n,l,a,mt[55][55],rez[DN][DN],aux[DN][DN],pot[DN][DN],v[DN];

void inm(int a[DN][DN],int b[DN][DN],int r[DN][DN]) {
    for(int i=1; i<=l; ++i) for(int j=1; j<=l; ++j) {
        int c=0;
        for(int k=1; k<=l; ++k) c=(c+1LL*a[i][k]*b[k][j])%MOD;
        r[i][j]=c;
    }
}
 
void lgput(int e) {
    int c[DN][DN];
    memset(rez,0,sizeof(rez));
    memcpy(c,pot,sizeof(pot));
    for(int i=1; i<=l; ++i) rez[i][i]=1;
    for(int i=0; (1<<i)<=e; ++i) {
        if((1<<i)&e) {
            inm(rez,c,aux);
            memcpy(rez,aux,sizeof(rez));
        }
        inm(c,c,aux);
        memcpy(c,aux,sizeof(aux));
    }
}

int main()
{
    ifstream f("sir5.in");
    ofstream g("sir5.out");
    f>>n>>l;
    for(int i=1; i<l; ++i) pot[i][i+1]=1;
    pot[l][1]=pot[l][l-1]=1;
    for(int i=1; i<=n; ++i) {
      f>>a;
    }
    return 0;
}
