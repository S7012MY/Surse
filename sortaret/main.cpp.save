#include <iostream>
#include <vector>
#include <fstream>
using namespace std;
ifstream f("sortaret.in")
ofstream g("sortaret.out")

vector <int> gr[50005];
int n,n1,m,x,i,j,y,v[50005],b[50005];

void dfs(int z)
{ v[z]=1;
  vector <int> :: iterator ir;
    for(ir=gr[z].begin();i!=gr[z].end() ;ir++)
        if(v[*ir]==0)  
            dfs(*ir);
  cit(z);    
}
void cit(q)
{
  b[n1]=z;
  n1--;
}
int main()
{
    f>>n>>m;
    for(i=1;i<=m;i++)
        {
          f>>x>>y;
          gr[x].push_back(y);
        }
    for(i=1;i<=n;i++)
        b[i]=0;
    n1=n;
    for(i=1;i<=n;i++)
          if()
    return 0;
}
