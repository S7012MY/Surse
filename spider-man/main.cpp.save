#include <iostream>
#include <fstream>
#include <cmath>
#define x first
#define y second
using namespace std;

typedef pair<double,double> per;

int n;
double xc,yc,r;
per a,b;

double dist(per a, per b) {
  return (b.x-a.x)*(b.x-a.x)+(b.y-a.y)*(b.y-a.y);
}

double f(double u) {
  per c;
  c.x=r*cos(u); c.y=r*sin(u);
  ret
}

int main()
{
    ifstream f("spider-man.in");
    ofstream g("spider-man.out");
    f>>xc>yc>>r;
    f>>a.x>>a.y>>b.x>>b.y;
    a.x-=xc; a.y-=yc;
    b.x-=xc; b.y-=yc;
    double ls=0.000000001,ld=acos(-1),ts,td;
    for(;fabs(ld-ls)>=0.00001;) {
      ts=(2.0*ls+ld)/3.0;
      td=(ls+2.0*ld)/3.0;
      if(f(ts)<f(td)) ls=ts;
      else ld=td;
    }
    return 0;
}
