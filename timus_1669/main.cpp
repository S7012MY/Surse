#include <iostream>
#include <string>
#include <vector>
#define DN 100005
using namespace std;

vector<int> gr[DN];
string inp[2];
int n,m;

int mk(int a,int b) {
    return n*(a-q)+b;
}

void oriz(string s) {
    for(int i=0; i<s.size(); ++i) {
        if(i && s[i]=='#' && s[i+1]=='#') {
            gr[]
        }
}

int main()
{
    cin>>n>>m;
    cin>>inp[1];
    oriz(inp[1]);
    return 0;
}
